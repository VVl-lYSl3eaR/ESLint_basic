import { lang, Parser } from '../../interface/parser.type';

export default class assParser implements Parser {
  langData: lang[];

  typeChecker(): boolean {
    
  }

  parser(): boolean {
  }

  maker(): string | Buffer {
  }
}
